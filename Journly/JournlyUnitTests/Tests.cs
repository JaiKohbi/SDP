using System;
using System.Diagnostics.Eventing.Reader;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace JournlyUnitTests
{
    [TestClass]
    public class Tests
    {
        //start a new Journal
        [TestMethod]
        public void TestMethod1()
        {
        }

        //show a journal table of active entries

        //show a journal table of all entries

        //record a new journal

        //delete a journal entry

        //modify a journal entry

        //hide a journal entry

        //search for journal entries by key word

        //search for journal entries by date

        //search for journal entries by date range
    }
}
